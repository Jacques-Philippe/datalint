#pragma once

/**
 * @brief Unit test datalint domain types and parsing infrastructure.
 *
 * The datalint::test namespace contains tests to validate:
 * - Raw input parsing abstractions
 * - Application descriptor resolution
 * - Layout validation and patching logic
 *
 * This namespace defines the test suite for the datalint library.
 */
namespace datalint::test {}
