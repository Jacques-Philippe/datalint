#include <gtest/gtest.h>
#include <sstream>
// #include <csv/reader.h>

TEST(CsvReader, ReadsSimpleRow)
{
    // std::istringstream input("a,b,c\n");

    // csv::Reader reader(input);
    // csv::Row row;

    // ASSERT_TRUE(reader.read_row(row));
    // ASSERT_EQ(row.size(), 3);
    // EXPECT_EQ(row[0], "a");
    // EXPECT_EQ(row[1], "b");
    // EXPECT_EQ(row[2], "c");

    ASSERT_TRUE(true); // Placeholder assertion
}
