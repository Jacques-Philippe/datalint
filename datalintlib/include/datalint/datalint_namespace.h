#pragma once

/**
 * @brief Core datalint domain types and parsing infrastructure.
 *
 * The datalint namespace contains:
 * - Raw input parsing abstractions
 * - Application descriptor resolution
 * - Layout validation and patching logic
 *
 * This namespace defines the public API surface of the datalint library.
 */
namespace datalint {}
